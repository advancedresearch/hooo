fn test_refl a -> a {
    use std::refl;
    x : a;
    let y = refl(x) : a;
    return y;
}

fn test_absurd true -> b {
    use std::refl;

    let x = refl() : all(a -> a);

    use std::all_pow_absurd;
    let y = all_pow_absurd(x) : b;

    return y;
}

